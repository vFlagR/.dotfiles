- defaults:
    link:
      relink: true
      create: true

- clean: ['~', "~/.config"]

- link:
     ~/.gitconfig-work: git/gitconfig-work

- shell:
  - [git submodule update --init --recursive, Installing submodules]